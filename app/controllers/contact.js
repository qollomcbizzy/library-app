import Ember from 'ember';
import Controller from '@ember/controller'
import { match, not,empty } from '@ember/object/computed'
export default Ember.Controller.extend({
 //if (empty('email') && empty('message')){
  //    disabled:true;
//  } else {
    //  disabled:false;
//  }
});
